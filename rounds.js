class Round {
  constructor() {
    this.categories = [];
    this.clues = []; 
  }

  createCategories() {
    let categoriesArray = Object.keys(data.categories)
  }

  createClues() {
    // array proto 
  }

  updateRound() {
    this.round++
  }
};